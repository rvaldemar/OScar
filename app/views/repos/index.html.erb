<div class="wrapper-main">
    <!-- Search form -->
  <%= render 'components/search-bar' %>
    <% @repos.each do |repo|%>
      <%= render 'components/repos_list', repo: repo %>
    <% end %>

</div>
