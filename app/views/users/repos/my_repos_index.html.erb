<div class="wrapper-main">
  <% if @repos.empty? %>
    <div id="new-post" class="top-margin">
      <div class="new-post-wrapper">
        <div class="form-group">
          <h1>Share your first repository!</h1>
        </div>
        <div>
          <form>

            <% @new_repos.each_with_index do |repo, i| %>

              <div class="repo">

                <input type="checkbox" name='<%= i %>' value="o que quero receber">
                <label for="<%= i %>"><%= repo.name %></label>

                <input class="input-hiden" type="text" name="name" value='<%= repo.name %>'><br>

                <input class="input-hiden" type="text" name="description" value='<%= repo.description %>'><br>

              </div>

            <% end %>

            <button type="submit">Subscribe</button>
          </form>

        </div>
        <div class="new-post-buttons">
          <button type="button" class="btn btn-danger" id="btn-new-post-cancel">Nice!</button>
        </div>
      </div>
    </div>

  <% else %>
    <!-- Search form -->
  <input class="form-control" type="text" placeholder="Search" aria-label="Search">

  <% @repos.each do |repo|%>
    <%= render 'components/repos_list', repo: repo %>
  <% end %>
<% end %>
</div>
